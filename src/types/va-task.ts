export interface VATask {
  [key: string]: string;
  // Known fields:
  // date_pst, va_name, va_id, creator_name, creator_id,
  // account_name, product_name, product_link, caption_text,
  // video_file_link, posting_date, va_status, posting_order
}
