export interface Assignment {
  [key: string]: string;
  // Known fields for type hints:
  // date_pst, creator_name, creator_id, account_name, 
  // daily_video_target, product_name, video_style, 
  // script_name, script_content, assignment_order, active
}
